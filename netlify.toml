[build]
  command = "npm run build -- --configuration=production"
  publish = "dist/fds/browser"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
